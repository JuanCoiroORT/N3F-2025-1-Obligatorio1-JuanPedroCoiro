@{
    ViewData["Title"] = "Login";
}

<h2>@ViewData["Title"]</h2>

@if(ViewBag.Message != null)
{
    <div class="alert alert-danger">
        @ViewBag.Message
    </div>
}

<form method="post">
    <div class="form-group">
        <label>Email</label>
        <input type="email" name="Email" class="form-control" required value="juan@mail.com"/>
    </div>
    <div>
        <label>Password</label>
        <input type="password" name="Password" class="form-control" required value="Vale2003"/>
    </div>
    <button type="submit" class="btn btn-primary">Login</button>
</form>